var _a;
// next.config.ts (refactor)
var allowed = ((_a = process.env.NEXT_PUBLIC_BASE_URL) !== null && _a !== void 0 ? _a : '')
    .split(',')
    .map(function (s) { return s.trim(); })
    .filter(Boolean);
var nextConfig = {
    experimental: {
        serverActions: {
            allowedOrigins: allowed,
        },
    },
    reactStrictMode: true,
};
module.exports = nextConfig;
